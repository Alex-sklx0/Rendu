# Cuando el backend de Carolina esté listo, apunta esta URL a su servicio real
VITE_API_BASE_URL=http://localhost:8000
# true = usa datos simulados en memoria/localStorage (frontend/src/api/mockClient.ts)
VITE_USE_MOCKS=true

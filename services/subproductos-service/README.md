# subproductos-service

**Responsable:** Carolina Ramirez Lotero (backend).

Servicio stub — solo expone `GET /health`. Reemplazar `src/index.js` con la lógica real
descrita en el contrato de API compartido: `/docs/api-contract.md`.

No modificar desde el agente de frontend (ver `/AGENTS.md`).

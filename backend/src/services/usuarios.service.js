/**
 * Service de usuarios — lógica de negocio pura, sin SQL directo
 * TODO (Carolina): implementar cuando los models estén listos
 *
 * Ejemplo de función a implementar:
 *   import bcrypt from 'bcrypt';
 *   import { crearUsuario } from '../models/usuario.model.js';
 *
 *   export const registrarUsuario = async ({ email, password }) => {
 *     const password_hash = await bcrypt.hash(password, 10);
 *     return crearUsuario({ email, password_hash });
 *   };
 */

/**
 * Service de subproductos — lógica de negocio pura, sin SQL directo
 * TODO (Carolina): implementar cuando los models estén listos
 *
 * Ejemplo:
 *   import { crearSubproducto } from '../models/subproducto.model.js';
 *
 *   export const registrarSubproducto = async (datos) => {
 *     // Las publicaciones empiezan siempre en estado 'borrador'
 *     return crearSubproducto({ ...datos, estado_publicacion: 'borrador', disponible: true });
 *   };
 */
